{
  "name": "music-mic-assets",
  "private": true,
  "description": "Asset toolchain only — used to compile Tailwind CSS into assets/main.css. Not part of the Rust app.",
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o assets/main.css --minify",
    "watch:css": "tailwindcss -i src/input.css -o assets/main.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^4.2.4",
    "@tailwindcss/cli": "^4.2.4"
  }
}
